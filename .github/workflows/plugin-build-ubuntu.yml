# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

# SPDX-FileCopyrightText: 2026 Felipe Reyes
# SPDX-FileCopyrightText: 2026 Kaito Udagawa <umireon@kaito.tokyo>
#
# SPDX-License-Identifier: Apache-2.0

# file: .github/workflows/plugin-build-ubuntu.yml
# description: Build the Plate Blur plugin .deb for Ubuntu x86_64.

on:
  workflow_call:
    inputs:
      environment:
        description: The environment name for the build jobs, or empty string for no environment
        required: false
        type: string
        default: ''
      VCPKG_BINARY_SOURCES:
        description: VCPKG_BINARY_SOURCES environment variable for the build jobs
        required: true
        type: string
      vcpkg-version:
        description: The version of vcpkg to clone.
        required: true
        type: string
      ort-version:
        description: 'ONNX Runtime release tag (e.g. v1.23.2). The release must ship onnxruntime-linux-x64-*.tgz.'
        required: true
        type: string

permissions: {}

jobs:
  build-ubuntu-x86_64:
    environment: ${{ inputs.environment }}
    runs-on: ubuntu-24.04
    timeout-minutes: 60

    permissions:
      contents: read

    env:
      CLEAN_BASH_ENV: '${{ github.workspace }}/.github/scripts/clean-env-ubuntu.bash'
      FILTERED_PATH: '/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin'
      ROOT_VCPKG_ROOT: '${{ github.workspace }}/vcpkg'
      VCPKG_BINARY_SOURCES: '${{ inputs.VCPKG_BINARY_SOURCES }}'

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Extract plugin buildspec
        id: buildspec
        uses: ./.github/actions/extract-buildspec

      - name: Set up ccache
        id: setup-ccache
        uses: ./.github/actions/setup-ccache

      - name: Checkout vcpkg
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          repository: microsoft/vcpkg
          ref: ${{ inputs.vcpkg-version }}
          persist-credentials: false
          path: ${{ env.ROOT_VCPKG_ROOT }}

      - name: Run bootstrap-vcpkg.sh
        shell: bash --noprofile --norc -euo pipefail -O nullglob {0}
        env:
          BASH_ENV: '${{ env.CLEAN_BASH_ENV }}'
          PATH: '${{ env.FILTERED_PATH }}'
          VCPKG_ROOT: '${{ env.ROOT_VCPKG_ROOT }}'
        run: |
          "$VCPKG_ROOT/bootstrap-vcpkg.sh" -disableMetrics

      - name: Install dependencies from apt (Ubuntu x86_64)
        shell: bash --noprofile --norc -euo pipefail -O nullglob {0}
        run: |
          APT_PACKAGES=(
            libgles2-mesa-dev
            libqt6svg6-dev
            libsimde-dev
            obs-studio
            qt6-base-dev
            qt6-base-private-dev
          )
          echo "man-db man-db/auto-update boolean false" | sudo debconf-set-selections
          sudo apt-get remove --purge -y man-db
          sudo add-apt-repository -y ppa:obsproject/obs-studio
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends "${APT_PACKAGES[@]}"

      - name: Install dependencies from vcpkg (Ubuntu x86_64)
        shell: bash --noprofile --norc -euo pipefail -O nullglob {0}
        env:
          BASH_ENV: '${{ env.CLEAN_BASH_ENV }}'
          PATH: '${{ env.FILTERED_PATH }}'
          VCPKG_ROOT: '${{ env.ROOT_VCPKG_ROOT }}'
        run: |
          "$VCPKG_ROOT/vcpkg" install --x-install-root=./.deps_vendor/vcpkg_installed --triplet=x64-linux-obs

      - name: Download prebuilt ONNX Runtime (Linux x86_64)
        shell: bash --noprofile --norc -euo pipefail -O nullglob {0}
        env:
          BASH_ENV: '${{ env.CLEAN_BASH_ENV }}'
          PATH: '${{ env.FILTERED_PATH }}'
          ORT_VERSION: ${{ inputs.ort-version }}
        run: |
          version="${ORT_VERSION#v}"
          asset="onnxruntime-linux-x64-${version}.tgz"
          url="https://github.com/microsoft/onnxruntime/releases/download/${ORT_VERSION}/${asset}"
          mkdir -p .deps_vendor
          curl -L --fail --retry 3 -o ".deps_vendor/${asset}" "$url"
          tar -C .deps_vendor -xzf ".deps_vendor/${asset}"
          rm -rf .deps_vendor/onnxruntime
          mv ".deps_vendor/onnxruntime-linux-x64-${version}" .deps_vendor/onnxruntime
          test -f .deps_vendor/onnxruntime/include/onnxruntime_cxx_api.h

      - name: Fetch plate detection model
        shell: bash --noprofile --norc -euo pipefail -O nullglob {0}
        env:
          BASH_ENV: '${{ env.CLEAN_BASH_ENV }}'
          PATH: '${{ env.FILTERED_PATH }}'
        run: |
          ./scripts/fetch_model.sh

      - name: Configure (Ubuntu x86_64)
        shell: bash --noprofile --norc -euo pipefail -O nullglob {0}
        env:
          BASH_ENV: '${{ env.CLEAN_BASH_ENV }}'
          PATH: '${{ env.FILTERED_PATH }}'
        run: |
          cmake --preset ubuntu-ci-x86_64

      - name: Build (Ubuntu x86_64)
        shell: bash --noprofile --norc -euo pipefail -O nullglob {0}
        env:
          BASH_ENV: '${{ env.CLEAN_BASH_ENV }}'
          PATH: '${{ env.FILTERED_PATH }}'
        run: |
          cmake --build --preset ubuntu-ci-x86_64

      - name: Package (Ubuntu x86_64)
        shell: bash --noprofile --norc -euo pipefail -O nullglob {0}
        env:
          BASH_ENV: '${{ env.CLEAN_BASH_ENV }}'
          PATH: '${{ env.FILTERED_PATH }}'
        working-directory: build_x86_64
        run: |
          cpack -G DEB

      - name: Upload Plugin Binary
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: ${{ steps.buildspec.outputs.NAME }}-pluginbinary-${{ runner.os }}-x86_64-${{ github.run_id }}
          path: |
            release/*-x86_64-linux-gnu.deb
            release/*-x86_64-linux-gnu-dbgsym.ddeb
          if-no-files-found: error
          retention-days: 7
